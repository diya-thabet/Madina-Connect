import React, { useState, useEffect, useRef } from 'react';
import { 
  LayoutDashboard, 
  Siren, 
  Bus, 
  Calendar, 
  BrainCircuit, 
  MapPin, 
  Wind, 
  Bell, 
  Menu, 
  X, 
  Send, 
  Activity,
  ShieldAlert,
  ThermometerSun,
  User
} from 'lucide-react';

// --- CONFIGURATION ---
const API_URLS = {
  // We use the REST ports we forwarded in the script
  MOBILITY: 'http://localhost:8000',
  PLANNER: 'http://localhost:8001',
  URGENCE: 'http://localhost:30090', 
  EVENTS: 'http://localhost:8082',
  // Air Quality is SOAP (hard for browser), so we might mock or wrap it
  AIR_QUALITY: 'http://localhost:30081' 
};

// --- MOCK DATA (Fallbacks if APIs are offline) ---
const MOCK_ALERTS = [
  { id: '1', type: 'Accident', description: 'Car collision near central park', status: 'PENDING', latitude: 36.8065, longitude: 10.1815, timestamp: '10 min ago' },
  { id: '2', type: 'Fire', description: 'Small brush fire reported', status: 'DISPATCHED', latitude: 36.8100, longitude: 10.1900, timestamp: '25 min ago' },
];

const MOCK_EVENTS = [
  { id: 1, title: 'City Marathon', date: '2025-12-10', location: 'Ave Habib Bourguiba' },
  { id: 2, title: 'Tech Expo', date: '2025-12-15', location: 'Expo Center' },
];

const MOCK_BUSES = [
  { id: 'BUS-101', route: 'Line 20', status: 'On Time', nextStop: 'Ariana' },
  { id: 'BUS-204', route: 'Line 5', status: 'Delayed', nextStop: 'Tunis Marine' },
];

// --- COMPONENTS ---

const SidebarItem = ({ icon: Icon, label, active, onClick }) => (
  <button
    onClick={onClick}
    className={`w-full flex items-center space-x-3 px-6 py-4 transition-all duration-200 ${
      active 
        ? 'bg-teal-500/10 text-teal-400 border-r-4 border-teal-400' 
        : 'text-slate-400 hover:bg-slate-800 hover:text-slate-100'
    }`}
  >
    <Icon size={20} />
    <span className="font-medium">{label}</span>
  </button>
);

const Card = ({ children, className = "" }) => (
  <div className={`bg-slate-800/50 backdrop-blur-md border border-slate-700/50 rounded-xl p-6 ${className}`}>
    {children}
  </div>
);

const Badge = ({ status }) => {
  const colors = {
    PENDING: 'bg-yellow-500/20 text-yellow-400 border-yellow-500/50',
    DISPATCHED: 'bg-blue-500/20 text-blue-400 border-blue-500/50',
    RESOLVED: 'bg-green-500/20 text-green-400 border-green-500/50',
    CRITICAL: 'bg-red-500/20 text-red-400 border-red-500/50',
  };
  return (
    <span className={`px-3 py-1 rounded-full text-xs font-bold border ${colors[status] || colors.PENDING}`}>
      {status}
    </span>
  );
};

// --- VIEWS ---

const DashboardView = ({ setActiveTab }) => {
  return (
    <div className="space-y-6">
      <header className="mb-8">
        <h2 className="text-3xl font-bold text-white">City Overview</h2>
        <p className="text-slate-400">Real-time metrics for Madina Connect</p>
      </header>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <Card className="flex items-center space-x-4 bg-gradient-to-br from-red-900/40 to-slate-900">
          <div className="p-3 bg-red-500/20 rounded-lg text-red-400">
            <Siren size={24} />
          </div>
          <div>
            <p className="text-slate-400 text-sm">Active Alerts</p>
            <p className="text-2xl font-bold text-white">12</p>
          </div>
        </Card>

        <Card className="flex items-center space-x-4">
          <div className="p-3 bg-teal-500/20 rounded-lg text-teal-400">
            <Wind size={24} />
          </div>
          <div>
            <p className="text-slate-400 text-sm">Air Quality</p>
            <p className="text-2xl font-bold text-white">42 <span className="text-sm font-normal text-teal-400">(Good)</span></p>
          </div>
        </Card>

        <Card className="flex items-center space-x-4">
          <div className="p-3 bg-blue-500/20 rounded-lg text-blue-400">
            <Bus size={24} />
          </div>
          <div>
            <p className="text-slate-400 text-sm">Active Buses</p>
            <p className="text-2xl font-bold text-white">156</p>
          </div>
        </Card>

        <Card className="flex items-center space-x-4">
          <div className="p-3 bg-purple-500/20 rounded-lg text-purple-400">
            <Calendar size={24} />
          </div>
          <div>
            <p className="text-slate-400 text-sm">Upcoming Events</p>
            <p className="text-2xl font-bold text-white">8</p>
          </div>
        </Card>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <div className="lg:col-span-2">
          <Card className="h-full">
            <div className="flex justify-between items-center mb-6">
              <h3 className="text-xl font-semibold text-white">Recent Emergency Reports</h3>
              <button 
                onClick={() => setActiveTab('emergency')}
                className="text-sm text-teal-400 hover:text-teal-300 transition-colors"
              >
                View All
              </button>
            </div>
            <div className="space-y-4">
              {MOCK_ALERTS.map((alert) => (
                <div key={alert.id} className="flex items-center justify-between p-4 bg-slate-700/30 rounded-lg border border-slate-700 hover:border-slate-600 transition-all">
                  <div className="flex items-center space-x-4">
                    <div className="p-2 bg-slate-800 rounded-full text-slate-300">
                      <ShieldAlert size={20} />
                    </div>
                    <div>
                      <h4 className="font-semibold text-slate-200">{alert.type}</h4>
                      <p className="text-sm text-slate-400">{alert.description}</p>
                    </div>
                  </div>
                  <Badge status={alert.status} />
                </div>
              ))}
            </div>
          </Card>
        </div>
        
        <div className="space-y-6">
           <Card className="bg-gradient-to-b from-indigo-900/20 to-slate-900">
              <h3 className="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <BrainCircuit className="text-indigo-400" /> AI Planner
              </h3>
              <p className="text-slate-400 text-sm mb-4">
                Need help navigating the city or planning an event? Ask the Madina AI.
              </p>
              <button 
                onClick={() => setActiveTab('planner')}
                className="w-full py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg transition-all"
              >
                Start Chat
              </button>
           </Card>

           <Card>
              <h3 className="text-lg font-semibold text-white mb-4 flex items-center gap-2">
                <ThermometerSun className="text-orange-400" /> Environment
              </h3>
              <div className="space-y-3">
                <div className="flex justify-between text-sm">
                  <span className="text-slate-400">Temperature</span>
                  <span className="text-white">24°C</span>
                </div>
                <div className="flex justify-between text-sm">
                  <span className="text-slate-400">Humidity</span>
                  <span className="text-white">45%</span>
                </div>
                <div className="flex justify-between text-sm">
                  <span className="text-slate-400">CO2 Levels</span>
                  <span className="text-teal-400 font-bold">Low</span>
                </div>
              </div>
           </Card>
        </div>
      </div>
    </div>
  );
};

const EmergencyView = () => {
  const [alerts, setAlerts] = useState(MOCK_ALERTS);
  const [formData, setFormData] = useState({
    type: 'Accident',
    description: '',
    latitude: 36.8065,
    longitude: 10.1815,
    sender_cin: '12345678'
  });
  const [loading, setLoading] = useState(false);

  // In a real scenario, useEffect to fetch alerts from API_URLS.URGENCE

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);

    // Simulate API Call
    setTimeout(() => {
      const newAlert = {
        id: Math.random().toString(),
        ...formData,
        status: 'PENDING',
        timestamp: 'Just now'
      };
      setAlerts([newAlert, ...alerts]);
      setLoading(false);
      setFormData({ ...formData, description: '' });
      alert("Alert Sent Successfully via Gateway!");
    }, 1000);

    /* // REAL FETCH IMPLEMENTATION:
    try {
      const response = await fetch(`${API_URLS.URGENCE}/api/alerts`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(formData)
      });
      // Handle response...
    } catch (err) { console.error(err); }
    */
  };

  return (
    <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 h-[calc(100vh-8rem)]">
      {/* List */}
      <div className="lg:col-span-2 overflow-y-auto pr-2 custom-scrollbar">
        <h2 className="text-2xl font-bold text-white mb-6">Active Emergencies</h2>
        <div className="grid gap-4">
          {alerts.map((alert) => (
            <Card key={alert.id} className="hover:bg-slate-800/80 transition-all cursor-pointer group">
              <div className="flex justify-between items-start">
                <div className="flex gap-4">
                  <div className={`p-3 rounded-xl ${
                    alert.type === 'Fire' ? 'bg-orange-500/20 text-orange-500' : 'bg-red-500/20 text-red-500'
                  }`}>
                    <Siren size={24} />
                  </div>
                  <div>
                    <h3 className="text-lg font-semibold text-white group-hover:text-teal-400 transition-colors">
                      {alert.type}
                    </h3>
                    <p className="text-slate-400 text-sm mt-1">{alert.description}</p>
                    <div className="flex items-center gap-4 mt-3 text-xs text-slate-500">
                      <span className="flex items-center gap-1"><MapPin size={12}/> {alert.latitude.toFixed(4)}, {alert.longitude.toFixed(4)}</span>
                      <span className="flex items-center gap-1"><User size={12}/> CIN: ***{alert.sender_cin?.slice(-3)}</span>
                    </div>
                  </div>
                </div>
                <div className="flex flex-col items-end gap-2">
                  <Badge status={alert.status} />
                  <span className="text-xs text-slate-500">{alert.timestamp}</span>
                </div>
              </div>
            </Card>
          ))}
        </div>
      </div>

      {/* Form */}
      <div>
        <Card className="sticky top-0 border-teal-500/30">
          <h3 className="text-xl font-bold text-white mb-4">Report Emergency</h3>
          <form onSubmit={handleSubmit} className="space-y-4">
            <div>
              <label className="block text-sm text-slate-400 mb-1">Type</label>
              <select 
                className="w-full bg-slate-900 border border-slate-700 rounded-lg p-2.5 text-white focus:ring-2 focus:ring-teal-500 outline-none"
                value={formData.type}
                onChange={(e) => setFormData({...formData, type: e.target.value})}
              >
                <option value="Accident">Accident</option>
                <option value="Fire">Fire</option>
                <option value="Medical">Medical</option>
                <option value="Security">Security</option>
              </select>
            </div>
            
            <div>
              <label className="block text-sm text-slate-400 mb-1">Description</label>
              <textarea 
                rows="3"
                className="w-full bg-slate-900 border border-slate-700 rounded-lg p-2.5 text-white focus:ring-2 focus:ring-teal-500 outline-none resize-none"
                placeholder="Describe the situation..."
                value={formData.description}
                onChange={(e) => setFormData({...formData, description: e.target.value})}
                required
              />
            </div>

            <div className="grid grid-cols-2 gap-4">
              <div>
                <label className="block text-sm text-slate-400 mb-1">Lat</label>
                <input 
                  type="number" step="0.0001"
                  className="w-full bg-slate-900 border border-slate-700 rounded-lg p-2.5 text-white outline-none"
                  value={formData.latitude}
                  readOnly
                />
              </div>
              <div>
                <label className="block text-sm text-slate-400 mb-1">Long</label>
                <input 
                  type="number" step="0.0001"
                  className="w-full bg-slate-900 border border-slate-700 rounded-lg p-2.5 text-white outline-none"
                  value={formData.longitude}
                  readOnly
                />
              </div>
            </div>

            <button 
              type="submit" 
              disabled={loading}
              className="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-3 rounded-lg transition-all flex justify-center items-center gap-2"
            >
              {loading ? 'Sending...' : <><ShieldAlert size={18} /> REPORT NOW</>}
            </button>
            <p className="text-xs text-center text-slate-500">
              Your location is automatically detected.
            </p>
          </form>
        </Card>
      </div>
    </div>
  );
};

const PlannerView = () => {
  const [messages, setMessages] = useState([
    { id: 1, sender: 'bot', text: 'Hello! I am the Madina AI. How can I assist you today?' }
  ]);
  const [input, setInput] = useState('');
  const [isTyping, setIsTyping] = useState(false);
  const messagesEndRef = useRef(null);

  const scrollToBottom = () => {
    messagesEndRef.current?.scrollIntoView({ behavior: "smooth" });
  };

  useEffect(scrollToBottom, [messages]);

  const handleSend = () => {
    if (!input.trim()) return;

    const userMsg = { id: Date.now(), sender: 'user', text: input };
    setMessages(prev => [...prev, userMsg]);
    setInput('');
    setIsTyping(true);

    // Simulate LLM response
    setTimeout(() => {
      const botMsg = { 
        id: Date.now() + 1, 
        sender: 'bot', 
        text: `I have analyzed your request: "${input}". Based on current city data, traffic is light on Avenue Mohamed V. I recommend taking Bus Line 20.` 
      };
      setMessages(prev => [...prev, botMsg]);
      setIsTyping(false);
    }, 2000);
  };

  return (
    <div className="h-[calc(100vh-8rem)] flex flex-col">
       <header className="mb-4">
        <h2 className="text-2xl font-bold text-white">AI City Planner</h2>
        <p className="text-slate-400">Powered by Gemini & Madina Gateway</p>
      </header>

      <div className="flex-1 bg-slate-800/50 backdrop-blur-md rounded-2xl border border-slate-700 flex flex-col overflow-hidden">
        {/* Chat Area */}
        <div className="flex-1 overflow-y-auto p-6 space-y-4">
          {messages.map((msg) => (
            <div key={msg.id} className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}>
              <div className={`max-w-[80%] p-4 rounded-2xl ${
                msg.sender === 'user' 
                  ? 'bg-indigo-600 text-white rounded-br-none' 
                  : 'bg-slate-700 text-slate-200 rounded-bl-none'
              }`}>
                <p>{msg.text}</p>
              </div>
            </div>
          ))}
          {isTyping && (
             <div className="flex justify-start">
              <div className="bg-slate-700 px-4 py-2 rounded-2xl rounded-bl-none">
                <div className="flex space-x-2">
                  <div className="w-2 h-2 bg-slate-400 rounded-full animate-bounce"></div>
                  <div className="w-2 h-2 bg-slate-400 rounded-full animate-bounce delay-75"></div>
                  <div className="w-2 h-2 bg-slate-400 rounded-full animate-bounce delay-150"></div>
                </div>
              </div>
            </div>
          )}
          <div ref={messagesEndRef} />
        </div>

        {/* Input Area */}
        <div className="p-4 bg-slate-900/50 border-t border-slate-700">
          <div className="flex gap-4">
            <input 
              type="text" 
              className="flex-1 bg-slate-800 border-none rounded-xl px-4 py-3 text-white focus:ring-2 focus:ring-indigo-500 outline-none"
              placeholder="Ask about traffic, events, or emergencies..."
              value={input}
              onChange={(e) => setInput(e.target.value)}
              onKeyPress={(e) => e.key === 'Enter' && handleSend()}
            />
            <button 
              onClick={handleSend}
              className="bg-indigo-600 hover:bg-indigo-700 text-white p-3 rounded-xl transition-colors"
            >
              <Send size={20} />
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

const MobilityView = () => (
  <div className="space-y-6">
    <h2 className="text-2xl font-bold text-white">Smart Mobility</h2>
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
       <Card>
          <h3 className="text-lg font-semibold text-white mb-4">Live Bus Tracking</h3>
          <div className="space-y-4">
            {MOCK_BUSES.map((bus) => (
               <div key={bus.id} className="flex items-center justify-between p-4 bg-slate-700/30 rounded-lg border border-slate-700">
                  <div className="flex items-center gap-4">
                    <div className="bg-blue-500/20 p-2 rounded-lg text-blue-400">
                      <Bus size={20} />
                    </div>
                    <div>
                      <p className="font-bold text-white">{bus.route}</p>
                      <p className="text-xs text-slate-400">{bus.id}</p>
                    </div>
                  </div>
                  <div className="text-right">
                    <p className="text-sm text-teal-400 font-semibold">{bus.status}</p>
                    <p className="text-xs text-slate-500">Next: {bus.nextStop}</p>
                  </div>
               </div>
            ))}
          </div>
       </Card>
       <Card className="h-64 flex items-center justify-center bg-slate-800/30">
          <p className="text-slate-500 italic flex items-center gap-2">
            <MapPin size={16} /> Map Visualization Placeholder
          </p>
       </Card>
    </div>
  </div>
);

// --- MAIN LAYOUT ---

export default function App() {
  const [activeTab, setActiveTab] = useState('dashboard');
  const [isSidebarOpen, setSidebarOpen] = useState(true);

  const renderContent = () => {
    switch (activeTab) {
      case 'dashboard': return <DashboardView setActiveTab={setActiveTab} />;
      case 'emergency': return <EmergencyView />;
      case 'planner': return <PlannerView />;
      case 'mobility': return <MobilityView />;
      default: return <DashboardView setActiveTab={setActiveTab} />;
    }
  };

  return (
    <div className="flex h-screen bg-slate-900 text-slate-100 font-sans overflow-hidden">
      {/* Sidebar */}
      <aside className={`${isSidebarOpen ? 'w-64' : 'w-20'} bg-slate-900 border-r border-slate-800 flex flex-col transition-all duration-300 z-20`}>
        <div className="p-6 flex items-center gap-3">
          <div className="w-8 h-8 bg-gradient-to-tr from-teal-400 to-indigo-500 rounded-lg flex items-center justify-center shadow-lg shadow-teal-500/20">
            <Activity size={18} className="text-white" />
          </div>
          {isSidebarOpen && <h1 className="text-xl font-bold tracking-tight text-white">Madina<span className="text-teal-400">Connect</span></h1>}
        </div>

        <nav className="flex-1 py-6 space-y-2">
          <SidebarItem 
            icon={LayoutDashboard} 
            label={isSidebarOpen ? "Dashboard" : ""} 
            active={activeTab === 'dashboard'} 
            onClick={() => setActiveTab('dashboard')} 
          />
          <SidebarItem 
            icon={Siren} 
            label={isSidebarOpen ? "Emergency" : ""} 
            active={activeTab === 'emergency'} 
            onClick={() => setActiveTab('emergency')} 
          />
          <SidebarItem 
            icon={BrainCircuit} 
            label={isSidebarOpen ? "AI Planner" : ""} 
            active={activeTab === 'planner'} 
            onClick={() => setActiveTab('planner')} 
          />
          <SidebarItem 
            icon={Bus} 
            label={isSidebarOpen ? "Mobility" : ""} 
            active={activeTab === 'mobility'} 
            onClick={() => setActiveTab('mobility')} 
          />
          <SidebarItem 
            icon={Calendar} 
            label={isSidebarOpen ? "Events" : ""} 
            active={activeTab === 'events'} 
            onClick={() => setActiveTab('events')} 
          />
        </nav>

        <div className="p-4 border-t border-slate-800">
           <div className="flex items-center gap-3 p-2 rounded-lg bg-slate-800/50">
              <div className="w-8 h-8 rounded-full bg-slate-700 flex items-center justify-center text-xs font-bold">DH</div>
              {isSidebarOpen && (
                <div className="overflow-hidden">
                  <p className="text-sm font-semibold truncate">Dhia</p>
                  <p className="text-xs text-slate-500 truncate">Admin</p>
                </div>
              )}
           </div>
        </div>
      </aside>

      {/* Main Content */}
      <main className="flex-1 flex flex-col relative overflow-hidden bg-gradient-to-br from-slate-900 via-slate-900 to-slate-800">
        {/* Top Header */}
        <header className="h-16 border-b border-slate-800 flex items-center justify-between px-8 bg-slate-900/50 backdrop-blur-sm z-10">
          <button onClick={() => setSidebarOpen(!isSidebarOpen)} className="text-slate-400 hover:text-white transition-colors">
            {isSidebarOpen ? <X size={24} /> : <Menu size={24} />}
          </button>

          <div className="flex items-center gap-6">
            <div className="flex items-center gap-2 text-xs text-emerald-500 bg-emerald-500/10 px-3 py-1 rounded-full border border-emerald-500/20">
              <span className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
              System Operational
            </div>
            <button className="relative text-slate-400 hover:text-white transition-colors">
              <Bell size={20} />
              <span className="absolute -top-1 -right-1 w-2 h-2 bg-red-500 rounded-full"></span>
            </button>
          </div>
        </header>

        {/* Content Scroll Area */}
        <div className="flex-1 overflow-auto p-8 custom-scrollbar">
          <div className="max-w-7xl mx-auto">
            {renderContent()}
          </div>
        </div>
      </main>

      {/* Tailwind Custom Styles for Scrollbar */}
      <style>{`
        .custom-scrollbar::-webkit-scrollbar {
          width: 8px;
        }
        .custom-scrollbar::-webkit-scrollbar-track {
          background: rgba(30, 41, 59, 0.5); 
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
          background: rgba(71, 85, 105, 0.8); 
          border-radius: 4px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
          background: rgba(94, 114, 141, 1); 
        }
      `}</style>
    </div>
  );
}